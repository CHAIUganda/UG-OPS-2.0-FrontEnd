import React from 'react';
import PropTypes from 'prop-types';

const ConferenceFacilitiesSpecifications = ({
  setSuccessFeedback,
  setError,
// eslint-disable-next-line arrow-body-style
}) => {
  return (
    <>
      
    </>
  );
};

ConferenceFacilitiesSpecifications.propTypes = {
  setSuccessFeedback: PropTypes.func,
  setError: PropTypes.func,
};

export default ConferenceFacilitiesSpecifications;
